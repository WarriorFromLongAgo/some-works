<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orhon.smartcampus.modules.attendance.mapper.AttendanceTeacherLeaveExamineMapper">
    <insert id="insertAttendanceTeacherLeaveExamine">
        insert into attendance_teacher_leave_examine (attendance_teacher_leave_id, parent_user_id) values (#{id},#{parent_user_id})
    </insert>
    <update id="updateAttendanceTeacherLeaveExamine">
        update attendance_teacher_leave_examine set parent_user_id = #{parent_user_id} where attendance_teacher_leave_id = #{id}
    </update>
</mapper>
